name: CI Pipeline

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - - name: Build Docker Image
          run: docker build -t yogesh-demo:${{ github.run_number }} .

      - name: List Docker Images
        run: docker images